---
---

Bring the Demo to the resource-first configuration: Azure SQL over Google federation on Cloud Run as the main path, a managed identity variant for Azure hosts, and an optional Blob Storage test file downloaded through `WorkloadIdentityTokenCredential`.
